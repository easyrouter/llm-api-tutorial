# Open questions / 待澄清事项

The PRD (`docs/prd/`) answered most clarification items. The following are **still open** and
are tracked here so development can proceed under explicit assumptions. Each item names the
assumption currently built into the code and where it lives (`TODO(IT …)` markers).

| id    | question                                                                                                                                                                                                                                                                                                                                                                                                 | current assumption in code                                                                                                                                                                                                                                                                                | where                                                                                    |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Q-14  | PRD #14 answer is truncated ("能不能用户在…"). How are API keys issued — self-service from the gateway portal, or pushed via SSO / key-management?                                                                                                                                                                                                                                                       | Self-service: user pastes the key; tool validates format only, never stores it. If SSO issuance is possible later, add a `keyProvider` step before `configure`.                                                                                                                                           | `guide::validate_api_key`, Verify screen                                                 |
| Q-18  | Which documentation site (PRD #18 "指定文档网站")? URL + whether it can serve `index.json` + Markdown                                                                                                                                                                                                                                                                                                    | placeholder `https://docs.example.com/seedrouter-onboarding`; site must publish `{baseUrl}/{lang}/index.json` + `{baseUrl}/{lang}/<path>.md` for `zh-CN`/`en` (contract: `docs/ARCHITECTURE.md` §5.1, `src-tauri/resources/docs/README.md`); bundled bilingual fallback used when unreachable             | `app-config.json` → `docs`                                                               |
| Q-16  | Telemetry endpoint, retention, approval owner                                                                                                                                                                                                                                                                                                                                                            | disabled until `telemetry.endpoint` is set; payload schema in `docs/ARCHITECTURE.md` §7                                                                                                                                                                                                                   | `app-config.json` → `telemetry`                                                          |
| Q-12  | Signing: who holds the Windows Authenticode cert and Apple Developer ID? CI secrets or manual signing?                                                                                                                                                                                                                                                                                                   | release workflow signs automatically once the secrets exist (no workflow edit needed); unsigned CI bundles stay smoke tests only. Secret names + what each turns on: `docs/RELEASE.md`                                                                                                                    | `.github/workflows/release.yml`, `docs/RELEASE.md`                                       |
| Q-12a | **TBD — decide before purchase.** Windows code-signing key form: an exportable `.pfx`, or a hardware token / cloud signing service (Azure Trusted Signing, DigiCert KeyLocker, SSL.com eSigner)? Since June 2023 CA/B Forum rules put OV/EV keys on FIPS-140-2 hardware, so a `.pfx` may not even be purchasable. Also: OV or EV — OV must accumulate SmartScreen reputation, EV is trusted immediately. | The wired flow assumes an exportable `.pfx` (base64 in `WINDOWS_CERTIFICATE`, imported and referenced by thumbprint). A hardware/cloud key cannot produce one: the Windows signing step must then be swapped for `bundle.windows.signCommand` invoking the vendor's tool. macOS is unaffected either way. | `.github/workflows/release.yml` (step _Prepare Windows code signing_), `docs/RELEASE.md` |
| Q-CC1 | CC Switch distribution: pin a version + SHA-256 for intranet mirror, or always latest GitHub release?                                                                                                                                                                                                                                                                                                    | latest release from `farion1231/cc-switch` GitHub API, hash read from release assets when present; `intranetMirror` empty. When set, the mirror must serve `<intranetMirror>/latest.json` (`docs/ARCHITECTURE.md` §5.0.1)                                                                                 | `app-config.json` → `ccSwitch`                                                           |
| Q-FU1 | Codex Fast UI toolkit (ADR-0007) lifecycle: who rebuilds it when OpenAI ships a Codex build the patcher no longer recognises, and how does the new archive reach us — bundled resource (current) or an intranet download like the CC Switch installer?                                                                                                                                                   | bundled as `src-tauri/resources/codex-fast-ui/*.zip`, pinned by SHA-256 in `fast_ui::TOOLKIT_SHA256`, tested against `OpenAI.Codex 26.721.11231.0`; on any other build the patcher stops and the feature reports a failure instead of guessing                                                            | `src-tauri/src/fast_ui.rs`, `docs/adr/0007-codex-fast-ui-toolkit.md`                     |
| Q-FU2 | Sign-off to redistribute an unofficial modification of the Codex client inside our installer (legal / IT). Since `v0.1.0-test.6` this is the only build we ship.                                                                                                                                                                                                                                         | shipped opt-in, Windows only, isolated copy + restore, caveat shown in both languages; the code is inert without the archive, so the decision is reversible by deleting one resource file                                                                                                                 | `docs/adr/0007-codex-fast-ui-toolkit.md`                                                 |
| Q-OC1 | Codex desktop client availability in the Microsoft Store per region: the Store API lists product `9PLM9XGG6VKS` (ChatGPT, contains Codex) for the CN market, but this is unconfirmed on a real CN-region machine. If it is not available, the "open region settings" button is the only in-app path.                                                                                                     | `checks::codex_app` offers `open_system_uri{ms_store_codex_app}` **and** `open_system_uri{windows_region_settings}`; the offline MSIX (`codexApp.windowsMsix*`) and the public download page remain as fallbacks                                                                                          | `app-config.json` → `codexApp`, `src-tauri/src/checks/codex_app.rs`                      |
| Q-OC2 | Offline Codex client install trusts the MSIX being Store-signed (`Add-AppxPackage` per user, no admin). No SHA-256 is published for `ChatGPT-x64.msix` / `ChatGPT.dmg` (~745 MB / ~650 MB), so hard rule 7's hash check cannot run — only https + the package signature. Should IT mirror the files with a pinned hash?                                                                                  | `installer::codex_app_release` returns `sha256: null`; `DownloadResult.verified` stays false; the package signature is checked by Windows / macOS at install time                                                                                                                                         | `src-tauri/src/install/installer.rs`                                                     |
| Q-OC3 | macOS one-click flows (`installer -pkg` via `osascript … with administrator privileges`, DMG mount + `ditto` into `/Applications`, rc-file PATH repair, `launchctl unsetenv`) are implemented from documentation and unit tests only — not yet exercised on macOS hardware.                                                                                                                              | Windows paths can be exercised on the dev machine; macOS needs a manual pass before the pilot (Apple silicon + Intel, standard vs admin user)                                                                                                                                                             | `src-tauri/src/remediate.rs`, `src-tauri/src/install/installer.rs`                       |
| Q-D1  | Exact wording / order of the guide's fault table A–G                                                                                                                                                                                                                                                                                                                                                     | assumed mapping in `src-tauri/src/diagnose/mod.rs` header                                                                                                                                                                                                                                                 | `diagnose/`                                                                              |
| Q-U1  | Exact "API 地址处理规则" from the guide (used by the URL preview)                                                                                                                                                                                                                                                                                                                                        | trailing `/` stripped; `#` suffix = literal; `/v1` kept; bare origin → `/v1` appended for the OpenAI shapes, kept as the root for `anthropic_messages` (Claude Code appends `/v1/messages` itself, so a `/v1` base earns the `anthropic_v1_suffix` warning)                                               | `guide::preview_url`                                                                     |
| Q-21  | Acceptance metrics + baseline (first-run success rate, mean duration, ticket volume)                                                                                                                                                                                                                                                                                                                     | not needed for build; telemetry events already carry step/status/duration to compute them                                                                                                                                                                                                                 | —                                                                                        |
| Q-22  | Pilot department & timeline                                                                                                                                                                                                                                                                                                                                                                              | —                                                                                                                                                                                                                                                                                                         | —                                                                                        |

Process: when IT answers, update `app-config.json` (or the intranet override), remove the
`TODO(IT …)` marker, and move the row to the _Resolved_ table below with the date.

## Resolved

| id                          | answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | date       |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| Q-1                         | Service gateway base URL is `https://seedrouter.net/v1` (`app-config.json` → `gateway.baseUrl`; still overridable per site)                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-08-16 |
| Q-M1                        | PM defaults from the Codex config.toml template: model `gpt-5.6-sol`, reasoning effort `medium`, provider name `SeedRouter` (`app-config.json` → `gateway`); all remain user-editable in the UI                                                                                                                                                                                                                                                                                                                                                           | 2026-08-16 |
| Q-M2                        | Claude Code preset: model `claude-sonnet-5`, gateway address `https://seedrouter.net` — the **root**, because Claude Code appends `/v1/messages` to `ANTHROPIC_BASE_URL` itself (`app-config.json` → `gateway.claudeCode`); Codex keeps `…/v1` + `gpt-5.6-sol`. Both remain user-editable in the UI                                                                                                                                                                                                                                                       | 2026-08-24 |
| PRD #4 / #7 / #17 (revised) | PM + test-team feedback (screenshots p1–p4): the tool **may** write `~/.codex/config.toml` automatically (explicit click, backup, restore); env-var conflicts get a **one-click clean-up** whose confirmation shows the **full** values; admin prompts are acceptable when the button is labelled and the prompt comes from the OS / installer (Node MSI/pkg, `reg delete` via UAC). Codex account paths: ChatGPT account → CC Switch "OpenAI Official" preset + sign-in; no account → custom provider. Recorded in ADR-0008; ADR-0003 superseded in part | 2026-08-19 |
